<%# Cabeçalho consistente — estático, fora do <turbo-frame id="match">.
    Idêntico nas 3 telas (mesma paleta, mesma marca). %>
<header class="hdr">
  <div class="hdr-brand">
    <span class="ball" aria-hidden="true"></span>
    <b>TORCEDOR MALUCO</b>
  </div>
  <div class="hdr-right">
    <%= render "matches/energy" %>
    <span class="hdr-tag">COPA 2026</span>
  </div>
</header>
