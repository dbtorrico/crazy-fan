<div class="max-w-lg mx-auto p-4">
  <p class="text-sm text-gray-500 mb-4">Pergunta <%= session.dig(:game, "current_index").to_i + 1 %> de 5</p>
  <h2 class="text-xl font-bold mb-6"><%= @question.enunciado %></h2>
  <div class="flex flex-col gap-3">
    <% @question.answers.each do |answer| %>
      <%= button_to answer.texto, answer_games_path, params: { answer_id: answer.id },
          class: "w-full py-4 px-4 bg-blue-100 rounded-lg text-left text-base font-medium min-h-[44px]" %>
    <% end %>
  </div>
</div>
